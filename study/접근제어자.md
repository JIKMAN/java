접근제어자

```java
int year;            // 같은 패키지 내에서만 사용 가능 (defalut)
   
public int year;     // 외부 클래스에서 전부 사용 가능

private int year;    // 같은 클래스 내에서만 사용 가능

protected int year;  // 상속 관계에서만 public처럼 사용 가능, 상속 관계가 아닌 클래스는 private
```



